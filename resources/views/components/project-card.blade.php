<div class="project">
    <a href="{{ $url }}" target="_blank" rel="noopener noreferrer">
        <img src="{{ $image }}" alt="{{ $title }}">
        <div class="project-content">
            <h3>{{ $title }}</h3>
            <p>{{ $description }}</p>
        </div>
    </a>
</div>

